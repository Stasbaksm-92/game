'use strict';

/* let options = {
    width: 1024,
    height: 1024,
    name: "test"
};

console.log(options.name);
options.bool = false; //задаем новое свойство
options.colors = {
    border: "black",
    bg: "red"
};

delete options.bool; //удаление свойств


console.log(options);

// циклы-----------------------------------------------------
for (let key in options) {
    console.log('Свойство ' + key + ' имеет значение' + options[key]);
}
console.log(Object.keys(options).length);

//массивы
let arr = ["first", 2, 3, "four", 5]; */
/* arr.pop(); //убрать последний єлемент
arr.push("5"); //добавить єлемент
arr.shift(); //убрать первій єлемент
arr.unshift("1"); //добавить элемент в начало
 */

//метод перебора массива
/* console.log(arr);
arr.forEach(function (item, i, mass) {
    console.log(i + ': ' + item + " (массив:" + mass + ")");
}); */

//чтоб перебрать елементі массива
/* for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
} */

/* let ans = prompt("", ""),
    arr = [];

arr = ans.split(',');
console.log(arr); */

//метод  join
/* let arr = ["asas", "asas", "asasas", "asasas"]
i = arr.join(', ');
console.log(i); */


//метод sort
/* let arr = [1, 15, 3],
    i = arr.sort(compareNum);

function compareNum(a, b) {
    return a - b;
}

console.log(arr); */